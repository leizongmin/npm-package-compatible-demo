{
  "private": true,
  "scripts": {
    "build-browser": "node build.js",
    "build": "npm run build-browser",
    "run-browser": "node bundle/app_browser.js",
    "run-node": "node app_node.js",
    "run": "npm run run-node && npm run run-browser"
  },
  "devDependencies": {
    "esbuild": "^0.17.8"
  },
  "dependencies": {
    "util": "^0.12.5"
  }
}
